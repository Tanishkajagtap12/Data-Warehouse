function loadContent(url) {
    // Get the iframe element
    var iframe = document.getElementById("contentFrame");
  
    // Set the src attribute to load the specified URL
    iframe.src = url;
  }
  